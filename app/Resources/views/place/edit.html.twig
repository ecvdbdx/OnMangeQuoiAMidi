{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editer un restaurant</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <input type="submit" value="Save" class="btn btn-primary"/>
    <a href="{{ path('place_show', {'place': place.id}) }}" class="btn btn-default">
        <i class="glyphicon glyphicon-th-list"></i> Retour au restaurant
    </a>
    {{ form_end(edit_form) }}
{% endblock %}
