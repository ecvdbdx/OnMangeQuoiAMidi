{% extends 'base.html.twig' %}

{% block body %}
    <h1>Menu creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="submit" value="Create" class="btn btn-primary"/>
    <a href="{{ path('place_show', {'place': menu.place.id}) }}" class="btn btn-default">Back to the place</a>

    {{ form_end(form) }}
{% endblock %}
